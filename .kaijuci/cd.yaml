# AUTOGENERATED: DO NOT EDIT
name: cd

on:
  tag:
    filter:
      - "v[0-9]+.[0-9]+.[0-9]+"

jobs:
  publish-container-prod:
    automate: dockerfile
    directives:
      - publish:
          - source:
              - deploy/prod/Dockerfile

      # - release -- todo: add goreleaser
